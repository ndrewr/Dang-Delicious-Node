extends layout

block content
  .inner
    h2 #{selectedTag || title}
    ul.tags
      each t in tags
        li.tag
          a.tag__link(href=`/tags/${t._id}` class=(t._id === tag ? 'tag__link' : ''))
            span.tag__text= t._id
            span.tag__count= t.count
